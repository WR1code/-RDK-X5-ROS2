没问题，你想要的是一个**可视化的开机引导菜单（GRUB Menu）**。只要稍微修改一下配置，每次开机时就会出现一个列表，让你清楚地选择进入 Ubuntu、Windows，或者直接进入 BIOS（UEFI）设置。

特别是在 Ubuntu 22.04 及以上版本中，出于安全考虑，系统默认禁用了对其他操作系统的探测，这会导致 Windows 选项在开机菜单中消失。同时，对于像拯救者这样较新的 UEFI 机型，BIOS 选项其实已经被 GRUB 原生支持了。

你可以按照以下步骤来调出并完善这个菜单：

### 1. 修改 GRUB 配置文件

打开终端（`Ctrl` + `Alt` + `T`），输入以下命令编辑 GRUB 配置：

```bash
sudo nano /etc/default/grub

```

在打开的文件中，找到并修改（或添加）以下几个关键参数：

* **显示菜单**：找到 `GRUB_TIMEOUT_STYLE`，如果它的值是 `hidden`，请将其改为 `menu`。
```text
GRUB_TIMEOUT_STYLE=menu

```


* **设置等待时间**：找到 `GRUB_TIMEOUT`，将其设置为你想要的倒计时秒数（比如 10 秒），给足你选择系统的时间。
```text
GRUB_TIMEOUT=10

```


* **开启 Windows 探测（非常重要）**：在文件的末尾，手动添加下面这一行，允许 GRUB 扫描并添加 Windows 启动项。
```text
GRUB_DISABLE_OS_PROBER=false

```



修改完成后，按 `Ctrl + O` 保存，按 `Enter` 确认，最后按 `Ctrl + X` 退出编辑器。

### 2. 更新并生效配置

在终端中运行更新命令，让刚才的修改生效并重新扫描系统：

```bash
sudo update-grub

```

在运行这个命令时，注意观察终端的输出。你通常会看到类似这样的信息，这说明配置成功了：

* `Found Windows Boot Manager on...` （成功找到了 Windows）
* `Adding boot menu entry for UEFI Firmware Settings...` （成功添加了 BIOS/UEFI 选项）

### 3. 重启看效果

现在你可以重启电脑了。开机时，你将看到一个带有选项的黑底白字菜单，通常包含：

1. **Ubuntu** (默认启动项)
2. **Advanced options for Ubuntu** (Ubuntu 高级选项，用于内核恢复等)
3. **Windows Boot Manager** (你的 Windows 系统)
4. **UEFI Firmware Settings** (这就是你要的 **BIOS** 选项)

通过键盘上下方向键选择，回车即可进入对应的系统或 BIOS 界面。
