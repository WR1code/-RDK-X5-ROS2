<img width="442" height="345" alt="14abcdd4-09d8-418f-b489-6f33777c82f0" src="https://github.com/user-attachments/assets/baf49e88-a175-43f5-9c29-6aa752d89d8e" />

查步骤可以按这个顺序走：

先确认 /scan 本身有没有问题

ros2 topic echo /scan --once --field header

ros2 topic echo /scan --once --field range_max

我们看到 /scan.header.frame_id 是 lidar_link，这是正常的；雷达挂在车体上没错。

确认 RViz 固定坐标系

检查 RViz 配置：

Global Options -> Fixed Frame = map

LaserScan -> Use Fixed Frame = true

这说明 RViz 理论上会把激光转换到 map 下显示，不应该因为车转就整体乱飞。

查 TF 链是否完整

ros2 run tf2_ros tf2_echo map odom

ros2 run tf2_ros tf2_echo odom base_footprint

ros2 run tf2_ros tf2_echo base_footprint lidar_link

目标是确认链路存在：

map -> odom -> base_footprint -> lidar_link

链路能通，说明不是 frame 缺失。

对比 Gazebo 真实姿态和 ROS odom 姿态

Gazebo 真实 pose：

gz topic -e -t /world/default/dynamic_pose/info -n 1

ROS odom：

ros2 topic echo /odom --once --field pose.pose

关键是看 orientation/yaw 是否一致。我们发现旋转后 Gazebo 真实车体 yaw 和 /odom yaw 差很大。

定位根因

由第 4 步得出：问题不是雷达坐标，也不是 RViz，而是 DiffDrive 发布的轮式积分 /odom 在旋转时漂了。

SLAM/RViz 用这个错误的 odom -> base_footprint TF 去投影 /scan，所以激光图旋转后飞出去。

修复验证

修复后检查：

ros2 topic info /odom -v

ros2 run tf2_ros tf2_echo odom base_footprint

ros2 topic echo /scan --once --field header

然后在 RViz 里原地旋转小车，看激光是否还会被甩出地图。现在正常，说明修复有效。





问题不在 /scan 的 frame，也不主要在雷达量程。/scan 是 lidar_link，雷达挂在车体上是正常的。真正出问题的是之前 Gazebo DiffDrive 同时负责驱动和发布 /odom、/tf，

它发布的是轮子积分出来的里程计。小车旋转时四轮滑移/积分误差很大，导致 /odom -> base_footprint 的 yaw 和 Gazebo 真实车体 yaw 对不上。

结果就是：SLAM/RViz 拿“错误朝向”的 TF 去把激光投到 map，所以一开始正常，一旋转旧激光图就被甩到地图外面。

现在保留的修复是：

DiffDrive 只负责驱动车轮


它的轮式里程计改到 /wheel_odom、/wheel_tf

用 Gazebo OdometryPublisher 基于真实模型位姿发布 /odom 和 /tf

这就是现在旋转后正常的关键。


你这个问题的本质可以总结成一句话：

**激光雷达本身没有错，RViz 也没有错，错的是机器人姿态 TF 里的 `odom -> base_footprint` 旋转角度不准。SLAM/RViz 用了这个错误姿态去投影激光，所以车一转，激光点云就被“甩飞”到地图外面。**

---

## 1. `/scan.header.frame_id = lidar_link` 为什么是正常的？

雷达数据 `/scan` 是从雷达坐标系发出来的，所以它的 `frame_id` 应该是雷达自己的坐标系，例如：

```bash
ros2 topic echo /scan --once --field header
```

看到：

```yaml
frame_id: lidar_link
```

这是对的。

含义是：

```text
每一个 scan 点，最初都是在 lidar_link 坐标系下描述的。
```

比如雷达看到前方 1 米有障碍物，这个点最开始可以理解成：

```text
在 lidar_link 坐标系下：
x = 1.0
y = 0.0
```

但是 RViz 要把它画到地图上，不能只知道它在 `lidar_link` 里，还必须知道：

```text
lidar_link 当前在 map 里的位置和朝向
```

所以它需要完整 TF 链：

```text
map -> odom -> base_footprint -> lidar_link
```

只要这条链能通，RViz 才能把雷达点从 `lidar_link` 转到 `map` 下显示。

---

## 2. RViz Fixed Frame = map 是什么意思？

你设置：

```text
Global Options -> Fixed Frame = map
LaserScan -> Use Fixed Frame = true
```

意思是：

```text
RViz 最终会把所有东西都转换到 map 坐标系下显示。
```

所以 `/scan` 虽然原始坐标系是 `lidar_link`，但 RViz 显示时会执行类似这样的转换：

```text
scan 点 in lidar_link
        ↓ base_footprint -> lidar_link
scan 点 in base_footprint
        ↓ odom -> base_footprint
scan 点 in odom
        ↓ map -> odom
scan 点 in map
```

也就是：

```text
lidar_link -> base_footprint -> odom -> map
```

注意，这里面最关键的是：

```text
odom -> base_footprint
```

因为它表示小车当前在里程计坐标系里的位置和朝向。

如果这个 TF 的 yaw 错了，雷达点就会整体按照错误角度被旋转。

---

## 3. 为什么刚开始正常，一旋转就飞？

因为刚开始车还没有明显旋转时，`odom -> base_footprint` 的误差不明显。

比如真实小车朝向是：

```text
yaw = 0°
```

错误 odom 也差不多是：

```text
yaw = 0°
```

这时候激光投影还看起来正常。

但是你原地旋转之后，Gazebo 真实小车可能已经转了：

```text
真实 yaw = 90°
```

但是 DiffDrive 插件通过轮子积分算出来的 odom 可能只有：

```text
odom yaw = 40°
```

或者更严重：

```text
odom yaw = 130°
```

那么 RViz/SLAM 就会相信这个错误的 `odom yaw`。

于是雷达真实应该这样投影：

```text
真实车头方向：90°
激光点应该跟着转到正确方向
```

但系统实际这样投影：

```text
系统认为车头方向：40°
激光点被按照 40° 投影
```

结果就是：

```text
雷达点云和真实地图对不上
```

所以你看到的现象就是：

```text
小车一转，激光图整体偏出去、拖出去、甩出地图边界。
```

你截图里面红色线条被拉到外面，就是典型的 **TF 姿态错误导致激光投影错误**。

---

## 4. 为什么 DiffDrive 发布的 `/odom` 会错？

Gazebo 的 DiffDrive 插件通常会做两件事：

```text
1. 接收 /cmd_vel，驱动车轮转动
2. 根据轮子转动积分，发布 /odom 和 odom -> base_footprint TF
```

问题在第 2 点。

DiffDrive 的里程计是根据轮子转了多少来估计车体运动的。这个东西叫：

```text
wheel odometry
轮式里程计
```

它不是 Gazebo 里面车体的真实 pose，而是根据轮子转动推算出来的。

对于差速小车、四轮小车、仿真摩擦参数不完美的小车，尤其是原地旋转时，很容易出现：

```text
轮子在转
但是车体实际旋转量和轮子积分估计量不一致
```

比如：

```text
轮子积分认为车转了 90°
Gazebo 真实模型只转了 60°
```

或者反过来：

```text
Gazebo 真实模型转了 90°
轮子积分只认为转了 45°
```

这就会导致 `/odom` 里的 orientation/yaw 错误。

你前面做的第 4 步：

```bash
gz topic -e -t /world/default/dynamic_pose/info -n 1
ros2 topic echo /odom --once --field pose.pose
```

就是在对比：

```text
Gazebo 真实车体姿态
vs
ROS /odom 发布的估计姿态
```

你发现两个 yaw 差很大，所以根因就锁定了：

```text
DiffDrive 发布的 /odom 不可信
```

---

## 5. 为什么这不是雷达量程问题？

如果是雷达量程问题，通常表现是：

```text
点太远
点太少
点被裁剪
远处墙看不到
```

但你的问题是：

```text
车一旋转，原本对齐地图的激光整体旋转错位
旧激光点被甩出地图
```

这不是 `range_max` 导致的。

`range_max` 只决定雷达最远能看到多远，比如：

```bash
ros2 topic echo /scan --once --field range_max
```

它影响的是：

```text
能不能看到远处障碍物
```

但不会造成：

```text
整片激光图旋转错位
```

整片错位通常就是 TF 姿态问题。

---

## 6. 为什么这不是 `/scan.frame_id` 问题？

如果 `/scan.frame_id` 错了，比如雷达实际挂在车头，但你写成别的 frame，可能会导致：

```text
雷达整体有固定偏移
雷达方向不对
雷达点相对车体位置不对
```

但是你这里 `/scan.frame_id = lidar_link` 是合理的。

并且 TF 链能通：

```bash
ros2 run tf2_ros tf2_echo map odom
ros2 run tf2_ros tf2_echo odom base_footprint
ros2 run tf2_ros tf2_echo base_footprint lidar_link
```

说明：

```text
map -> odom -> base_footprint -> lidar_link
```

存在。

链路存在不代表数值正确。

这是重点：

```text
TF 能查到，只说明 frame 没断；
TF 数值对不对，要看 pose/yaw 是否和真实运动一致。
```

你这个问题就是：

```text
TF 链没断，但是 odom -> base_footprint 的 yaw 数值错了。
```

---

## 7. 原来的错误链路是什么？

你原来的系统大概是这样：

```text
/cmd_vel
  ↓
Gazebo DiffDrive
  ↓
驱动车轮
  ↓
根据轮子转动积分
  ↓
发布 /odom
  ↓
发布 odom -> base_footprint
  ↓
SLAM/RViz 用这个 TF 投影 /scan
```

问题发生在这里：

```text
根据轮子转动积分发布 /odom
```

因为轮式积分在旋转时误差很大。

所以错误链路可以画成：

```text
真实 Gazebo 小车 yaw = 90°
        ↓
DiffDrive 轮式积分算错
        ↓
/odom yaw = 40°
        ↓
odom -> base_footprint TF 错
        ↓
/scan 从 lidar_link 转到 map 时角度错
        ↓
RViz 里激光被甩出地图
```

---

## 8. 现在修复后的结构是什么？

你现在保留的修复是：

```text
DiffDrive 只负责驱动车轮
```

它不再负责主 `/odom` 和主 `/tf`。

然后把 DiffDrive 原本的轮式里程计改名，例如：

```text
/wheel_odom
/wheel_tf
```

也就是说，DiffDrive 仍然可以算轮子积分，但它不再污染主 TF 链。

新的主里程计由 Gazebo OdometryPublisher 发布：

```text
Gazebo 真实模型 pose
        ↓
OdometryPublisher
        ↓
/odom
        ↓
odom -> base_footprint
```

这个 `/odom` 是基于 Gazebo 里模型真实位姿发布的，所以更接近仿真真值。

修复后的链路是：

```text
/cmd_vel
  ↓
DiffDrive
  ↓
只负责驱动车轮，不发布主 /odom 和主 /tf

Gazebo model pose
  ↓
OdometryPublisher
  ↓
发布 /odom
  ↓
发布 odom -> base_footprint
  ↓
SLAM/RViz 用正确 TF 投影 /scan
```

所以现在旋转后正常，是因为：

```text
odom -> base_footprint 的 yaw 和 Gazebo 真实 yaw 一致了。
```

---

## 9. 为什么要把 DiffDrive 的 odom 改到 `/wheel_odom`？

这是为了避免两个节点同时发布同一个东西。

最危险的情况是：

```text
DiffDrive 发布 /odom
OdometryPublisher 也发布 /odom
```

或者：

```text
DiffDrive 发布 odom -> base_footprint
OdometryPublisher 也发布 odom -> base_footprint
```

这样 TF 会冲突。

RViz/SLAM/Nav2 不知道该信谁，可能这一帧用 DiffDrive 的，下一帧用 OdometryPublisher 的，表现就会非常混乱。

所以正确做法是：

```text
主 /odom 只能有一个可靠来源
主 odom -> base_footprint TF 只能有一个发布者
```

你现在的设计是：

```text
主 /odom：Gazebo OdometryPublisher
主 odom -> base_footprint：Gazebo OdometryPublisher
轮式里程计备份：/wheel_odom、/wheel_tf
```

这样结构就清楚了。

---

## 10. 为什么修复后 SLAM/RViz 正常？

因为 SLAM 和 RViz 的逻辑都是依赖 TF 的。

对于每一帧 `/scan`，系统都会问 TF：

```text
这个 scan 的时间点，lidar_link 在 map 里面在哪里？
```

然后用 TF 把激光点转换到 map 下。

修复前：

```text
lidar_link -> base_footprint 是对的
base_footprint -> odom 是错的
odom -> map 由 SLAM 修正
```

所以整体投影错。

修复后：

```text
lidar_link -> base_footprint 是对的
base_footprint -> odom 是对的
odom -> map 由 SLAM 修正
```

于是激光可以稳定贴在地图上。

---

## 11. 你这套排查顺序为什么是正确的？

你的排查顺序其实很标准：

### 第一步：查 `/scan`

```bash
ros2 topic echo /scan --once --field header
ros2 topic echo /scan --once --field range_max
```

确认：

```text
雷达 frame 正常
雷达数据本身正常
```

---

### 第二步：查 RViz

```text
Fixed Frame = map
LaserScan Use Fixed Frame = true
```

确认：

```text
RViz 显示逻辑正常
```

---

### 第三步：查 TF 链

```bash
ros2 run tf2_ros tf2_echo map odom
ros2 run tf2_ros tf2_echo odom base_footprint
ros2 run tf2_ros tf2_echo base_footprint lidar_link
```

确认：

```text
TF 链路没有断
```

---

### 第四步：对比真实姿态和 odom 姿态

```bash
gz topic -e -t /world/default/dynamic_pose/info -n 1
ros2 topic echo /odom --once --field pose.pose
```

确认：

```text
odom 的 yaw 和 Gazebo 真实 yaw 是否一致
```

这一部最关键。

因为它把问题从：

```text
可能是雷达
可能是 RViz
可能是 SLAM
可能是 TF
```

缩小到：

```text
就是 odom -> base_footprint 的姿态错了
```

---

## 12. 最终结论

你的问题不是：

```text
/scan.frame_id 错
RViz Fixed Frame 错
雷达量程错
map 坐标系错
```

真正的问题是：

```text
DiffDrive 发布的轮式积分 /odom 在旋转时 yaw 漂移严重。
```

它导致：

```text
odom -> base_footprint TF 错
```

进一步导致：

```text
SLAM/RViz 用错误车体朝向投影 /scan
```

所以一旋转：

```text
激光点云被投影到错误方向
地图看起来被甩飞
```

现在修复为：

```text
DiffDrive 只驱动车轮
DiffDrive 的轮式 odom 改到 /wheel_odom、/wheel_tf
Gazebo OdometryPublisher 用真实模型 pose 发布 /odom 和 /tf
```

这是正确的。

你现在旋转后激光不乱飞，说明：

```text
主 TF 链中的 odom -> base_footprint 已经恢复正确
```

也就是：

```text
map -> odom -> base_footprint -> lidar_link
```

这条链不仅“能通”，而且“数值也对”。
